---
  - hosts: all
    become: yes
    tasks:
      - name: upgrade system
        apt:
          update_cache: yes
          upgrade: full
      - name: install apache2 package
        apt:
          name: apache2
